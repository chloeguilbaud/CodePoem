package billing.implementation.exemple.domain.model;

public class PayerInformations extends Client {
    public PayerInformations(String name, String surname, String mail, String address) {
        super(name, surname, mail, address);
    }
}
