package billing.implementation.exemple.domain.model;

public class PayerDetails extends Client {
    public PayerDetails(String name, String surname, String mail, String address) {
        super(name, surname, mail, address);
    }
}
